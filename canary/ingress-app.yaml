apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rules
  labels:
    app: my-app  
spec:
  rules:
  - host: myapp.fbi.com
    http:
      paths:
      - backend:
          service:
            name: my-app
            port:
              number: 80
        path: /
        pathType: ImplementationSpecific