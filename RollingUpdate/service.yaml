apiVersion: v1
kind: Service
metadata:
  name: frontend-podinfo
  labels:
    app: frontend-podinfo
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 9898
    targetPort: http
  selector:
    app: frontend-podinfo